import styled from "styled-components"


export const StyledHeader = styled.header`
background: linear-gradient(black,grey,brown);
  padding: 10px;
  color: white;
  text-align: center;
  font-size: 28px; `